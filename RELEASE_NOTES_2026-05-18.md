# wechat-toast 发布说明

发布日期：2026-05-18

本次更新主要围绕 `exe` 交付体验、托盘后台运行能力，以及微信 4.x 的兼容性说明进行了完善，方便直接发给终端用户使用。

## 本次更新

- 新增一键打包脚本 `build.ps1`，可直接生成单文件 `exe`
- 新增自定义程序图标与版本资源：
  - `packaging/wechat-toast.ico`
  - `packaging/wechat-toast.png`
  - `packaging/version_info.txt`
- `exe` 版本信息中的 `FileDescription` 调整为 `wechat-toast`
- 程序启动后会主动提示“启动成功”
- 程序运行时会显示任务栏托盘图标
- 支持托盘右键退出，适合后台常驻使用
- 日志文件统一为 `wechat-toast.log`
- `README.md` 补充了打包方式、交付说明和已知问题

## 使用说明

- 适用系统：Windows 10 / 11
- 依赖环境：已安装并登录微信桌面版 4.x
- 推荐交付物：`dist\wechat-toast.exe`
- 启动后程序会在后台运行，可通过系统托盘图标查看运行状态并退出

## 已知限制

- 当微信当前停留在文章页、内置浏览器页或类似网页视图时，程序可能无法读取聊天会话列表，因此这段时间不会弹出新消息提醒
- 关闭该页面或切回聊天主界面后，提醒能力会恢复
- 当前版本点击通知后可以拉起微信主窗口，但还不能自动精确跳转到对应会话

## 适合告知用户的话术

可直接把 `wechat-toast.exe` 发给用户，用户双击即可运行。程序启动后会在后台监控微信新消息，并通过本地浮层或系统通知进行提醒；如果微信正停留在文章页或内置浏览器页，提醒可能会暂时失效，切回聊天主界面后即可恢复。
